\section{Planned Next Phase: Baseline PoC Implementation}
\label{sec:phase2_plan}

Phase II has now compared the candidate metrics using the criteria defined in
Section~\ref{sec:methodology_criteria}. The centralized Phase I artifacts are the evidence base,
so the analysis focused on metric behavior rather than additional dataset construction.
The four comparative criteria, C1 stability, C2 interpretability, C3 gaming resistance, and C4 long-term suitability,
are reported in Sections~\ref{sec:phase2_c1_stability},~\ref{sec:phase2_c2_interpretability},
\ref{sec:phase2_c3_gaming_resistance}, and~\ref{sec:phase2_c4_longterm_suitability}. The smart-contract
feasibility pass is reported in Section~\ref{sec:phase2_feasibility_scoring}, and the final metric selection is
reported in Section~\ref{sec:phase2_poc_metric_selection}. The next step is to instantiate the selected metric in
the baseline proof-of-concept implementation.

\subsection{Phase II execution plan}
\label{sec:phase2_execution}

\begin{enumerate}[leftmargin=*]
  \item Instantiate \texttt{M4\_developer\_share\_churn} inside the DESTINO-aligned metric-record blueprint in Section~\ref{sec:detailed_design_blueprint}.
  \item Implement the baseline PoC contract fields from Table~\ref{tab:selected_metric_record_fields}.
  \item Implement the minimal off-chain verifier that computes \texttt{share\_churn\_ppm} from \path{out/ownership_dev_shares_human_all.csv}.
  \item Build one replayable evidence packet containing the frozen window identifiers, numerator, denominator, encoded value, policy hash, and evidence hash.
  \item Test the PoC with one or more frozen release-window examples from the centralized Phase I dataset.
\end{enumerate}

\subsection{Decision gate criteria}
\label{sec:phase2_gate}

The selected PoC metric has passed the following gate:
\begin{itemize}[leftmargin=*]
  \item no feasibility exclusion criterion triggered under Section~\ref{sec:feasibility_rubric_full};
  \item acceptable comparative behavior under C1--C4 relative to alternatives;
  \item stable interpretation under bot-sensitivity and short-window robustness checks;
  \item reproducible extraction with no unresolved data-integrity issue.
\end{itemize}

This gate keeps implementation tied to the evidence, instead of choosing a metric because it is easy to compute
or attractive in one project only.
